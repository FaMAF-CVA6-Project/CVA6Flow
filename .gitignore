# Frozen development history
/docs/old_versions
/docs/parser_phases

# Python
__pycache__

# Testing: VCDs and listings are never committed
*.vcd
*.fst
*.list

# A tracer JSON in tests/ is what a sample is made from, not content: the
# samples themselves, the .sample.js files and samples.js, are committed.
tests/*.json

# make_CVA6Flow_oversized.py output, with the side files of a write it
# did not finish
oversized*.json*
